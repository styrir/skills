# Example Project

Inside this project:

```bash
setja prepare --hexis=product --query="summarize the most important product context"
```

`setja prepare` bootstraps any missing Setja scaffold automatically. Use `setja init` only as an optional scaffold/debug command when you want to seed or inspect the starter files explicitly.
