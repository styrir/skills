# Test fixtures

Public fixtures only. Private author corpus and evaluation outputs must never be stored here.

Use synthetic prose for unit tests. Real evaluation corpora live under `$VOICE_PROFILE_DIR`.
