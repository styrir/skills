Use a two-lane pipeline: stream Claude/Codex/Pi/OMP/Grok source files into a normalized, file-native ATIF trajectory and static HTML report; optionally project the same normalized events into OpenInference spans for an OTel-compatible collector. Store skill provenance separately from agent runtime identity: metadata.version for human semver, skillFolderHash/Git tree SHA for exact content, and run/model/harness metadata in ATIF extra fields and report indexes.
